namespace AtomicFramework.Update
{
    internal class JSONMod(string id, string version, string asset, string bepVersion)
    {
        internal string id = id;
        internal string version = version;
        internal string asset = asset;
        internal string bepVersion = bepVersion;
    }
}
